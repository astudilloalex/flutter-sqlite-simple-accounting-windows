import 'package:flutter/material.dart';

class PDFIncomeStatement extends StatelessWidget {
  const PDFIncomeStatement({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
